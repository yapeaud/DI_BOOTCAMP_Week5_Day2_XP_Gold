import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Exercise_2_Service_Workers_Notifications',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
